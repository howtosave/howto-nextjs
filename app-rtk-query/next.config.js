/** @type {import('next').NextConfig} */
//const withTranspileModules = require('next-transpile-modules')(['@libs/cms-request']);

const nextConfig = {
  reactStrictMode: true,
};

//module.exports = withTranspileModules(nextConfig);
module.exports = nextConfig;
